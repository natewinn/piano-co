= form_for @event do |f|
	= f.datetime_select :start_time
	= f.datetime_select :end_time
	= f.select :customer_id, Customer.all.collect { |a| [(a.first_name + " " + a.last_name), a.id]}
	= f.submit 

	