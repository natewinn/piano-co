.container-fluid
  .col-lg-12
    .col-lg-4
      %h2= "Welcome back, #{@company.user.first_name}"
      Company:
      = "#{@company.name}"
      %p
      Phone number:
      = "#{@company.phone_number}"
      %p 
      Address:
      %br
      = "#{@company.address_1}"      
      = "#{@company.address_2}"     
      %br
      = "#{@company.city}"
      = "#{@company.state}"
      = "#{@company.zip_code}"
      %p
      = link_to "Update", edit_company_path, :method => :get, :class => "button btn btn-danger"
    .col-lg-4
    .col-lg-4  